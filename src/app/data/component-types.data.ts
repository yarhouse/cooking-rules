import { ComponentType } from '../models/component-type.model';

// Local data file — not committed to the public repo.
// Populate with your own component type data matching the ComponentType interface.
// See src/app/models/component-type.model.ts for the expected shape.
export const COMPONENT_TYPES: ComponentType[] = [];
