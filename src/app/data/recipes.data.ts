import { Recipe } from '../models/recipe.model';

// Local data file — not committed to the public repo.
// Populate with your own recipe data matching the Recipe interface.
// See src/app/models/recipe.model.ts for the expected shape.
export const RECIPES: Recipe[] = [];
